import React, { Component } from "react";

class Buttons extends Component {
  state = {};
  render() {
    // creat like and delete buttons link like button to a count next to search bar
    return <p>buttons</p>;
  }
}

export default Buttons;
